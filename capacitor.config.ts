import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saro.deadlinetracker',
  appName: 'DeadlineTracker',
  webDir: 'www'
};

export default config;
